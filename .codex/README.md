# Red Panda Academy Context

This folder is the working handoff for AI-assisted implementation. The legacy Netlify deploy artifact remains in `netlify_files/` and should be treated as the source of truth until the React app reaches feature parity.

Start with:

- `project-context.md` for product, current architecture, and invariants.
- `implementation-plan.md` for the staged React conversion sequence.
- `migration-rules.md` for compatibility rules that should not drift.

The old `plans/` folder is preserved as received. These `.codex` files are the maintained implementation context going forward.
